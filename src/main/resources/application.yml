spring:
  application:
    name: "users"
  h2:
    console:
      enabled: true
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password:
    url: jdbc:h2:file:~/DbUser
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8080